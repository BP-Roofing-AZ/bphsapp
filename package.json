{
  "name": "bphsapp",
  "version": "1.0.0",
  "private": true,
  "description": "Blueprint Home Services CRM \u2014 Cloudflare Workers + D1 + R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'static frontend \u2014 no build step required'"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
